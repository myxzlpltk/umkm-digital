<div class="card text-dark">
    <img src="{{ asset('storage/logos/'.$seller->logo) }}" class="card-img-top" alt="{{ $seller->store_name }}">
    <div class="card-body">
        <h5 class="h4 card-title mb-0 text-truncate">{{ $seller->store_name }}</h5>
    </div>
</div>
